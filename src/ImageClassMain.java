public class ImageClassMain {

    String fileInput;
    // take input, use try except block from Exceptions to chek file. input through this class



}

/*

must have requirements:
Obviousily java
Polymorphism
Inheritance
Abstract class
Interface
Constructor
Exceptions

Use JavaSwing or Java Fx-- USE JAVA FX
Contain an action listner + Button
Text display
Reading from a text file and displaying elements from it
Labels and UI pop ups
Should be an interactable UI
A surprise element that uses any feature from JavaSwing or JavaFx
Classes that pass other classes as objects among each other in a multifile environment
Ability to read or write to a file.
 */

/*
Contains act listener n button to open image in the main frame (right side in the window)
Manages interactable ui layout w adjustable frames etc

action listener for import/open button
triggers try except for valid image files
ui pop ups if invalid image exception caught
layout setup
right side main image display
left side split pane (top preview n metadata, bottom empty square frame)
empty frame constrained to square (width bound to height etc)
tab on far left to show/hide left pane
text display for metadata
pulls text elements from file handler
has surprise element using a feature from javafx
sepia tone n drop shadow on hover etc
Connects theme manager and file handler (passes objects around)
*/
